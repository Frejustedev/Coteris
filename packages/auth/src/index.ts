/**
 * @coteris/auth — authentification, organisations et permissions.
 *
 * Le module `permissions` est pur et sans dépendance : c'est ce qui permet de
 * tester exhaustivement la matrice des rôles en mémoire.
 */

export * from './permissions'
